import path from "path";

export const TOUR_SAMPLE_JSON_PATH: string = path.join(
  __dirname,
  "../data/tours-sample.json"
);

export const USERS_SAMPLE_JSON_PATH: string = path.join(
  __dirname,
  "../data/users.json"
);

export const REVIEWS_SAMPLE_JSON_PATH: string = path.join(
  __dirname,
  "../data/reviews.json"
);
export const STATIC_FOLDER_PATH: string = path.join(__dirname, "../static");
